name: thesis_app
description: A flutter project created for thesis work.

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.35.0
  get: ^4.6.5

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter